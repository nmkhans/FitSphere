export const metadata = {
    title: "Dashboard - FitSphere",
    description: "FitSphere Dashboard for managing specialized members and gym activities.",
};

export default function DashboardLayout({ children }) {
    return children;
}
